---
import { parseHTML } from '@utils/utils.js'
const { module } = Astro.props

console.log('module in heading', module)
let active
let heading
let headingTag
let classes

if (module) {
    active = module.active
    classes = module.tailwindClasses?.toString()
    console.log('classes', classes)
    heading = module.heading
    // headingTag = module.headingTag.toString()
    // console.log('heading', heading, headingTag, typeof headingTag)
}

---


{
    active && (
        <Fragment set:html={heading} />
    )
}
